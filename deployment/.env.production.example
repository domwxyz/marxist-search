# Production Environment Configuration
# Copy this file to /opt/marxist-search/backend/.env and configure for production
#
# NOTE: Most settings have sensible defaults and don't need to be changed.
# The systemd service files already set the critical production paths.

# ============================================================================
# Application Environment
# ============================================================================
ENVIRONMENT=production
DEBUG=false

# ============================================================================
# API Configuration
# ============================================================================
API_HOST=0.0.0.0
API_PORT=8000

# ============================================================================
# Logging
# ============================================================================
LOG_LEVEL=INFO

# ============================================================================
# CORS Origins
# IMPORTANT: Uncomment and set your production domain(s) for CORS to work
# Replace yourdomain.com with your actual domain
# ============================================================================
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================================================
# Data Paths
# NOTE: These are already configured in the systemd service files.
# Only uncomment if you need to override the defaults for some reason.
# ============================================================================
# DATA_DIR=/var/lib/marxist-search
# DATABASE_PATH=/var/lib/marxist-search/articles.db
# INDEX_PATH=/var/lib/marxist-search/txtai
# CACHE_PATH=/var/lib/marxist-search/cache

# ============================================================================
# Performance Tuning (optional)
# ============================================================================
# SEARCH_THREAD_POOL_SIZE=4
# MAX_CONCURRENT_SEARCHES=10

# ============================================================================
# Rate Limiting (optional - primarily handled by Nginx)
# ============================================================================
# RATE_LIMIT_PER_MINUTE=100
