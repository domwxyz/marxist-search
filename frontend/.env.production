# Production Environment Configuration for Frontend
# This file ensures the production build uses the correct API URL

# Use relative URL so nginx can proxy /api/* to the backend
# This avoids CORS issues and allows the frontend and backend to be served from the same domain
REACT_APP_API_URL=/api/v1
